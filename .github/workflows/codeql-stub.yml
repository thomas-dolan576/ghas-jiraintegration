name: "CodeQL"
# Stub standing in for the real CodeQL workflow — this repo is private
# without GHAS, so actual scanning can't run here. Exists to validate the
# workflow_run trigger chain in vuln-jira-story.yml. Delete when copying
# the integration into a repo that has real CodeQL.
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CodeQL stub completed (no real scan on private repo without GHAS)"
